// types/index.d.ts
import { Theme } from '@mui/material/styles';
/**
 * Creates a custom MUI theme.
 * @param theme - The base theme to extend.
 * @returns A customized MUI theme.
 */
export declare function createMuiTheme(theme?: Theme): Theme;
